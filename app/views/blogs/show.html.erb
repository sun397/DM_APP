<body>
<a href="/users/<%= current_user.id %>" >
 <h1>Hello <%= current_user.email %></h1>
</a>

<div class="container-fluid">
 <div class="row">
  <center><p class="title"><%= @blog.title %></p></center>
  <p><%= @blog.image %></p>
  <p class="content"><%= @blog.content %></p>
  <p class="timestamp">Posted <%= time_ago_in_words(@blog.created_at) %> ago.</p>
 </div>
</div>

  <%= link_to "編集", edit_blog_path(@blog), class: "btn btn-primary" %>
  <%= link_to "削除", blog_path(@blog), method: :delete, class: "btn btn-primary"%>
</body>